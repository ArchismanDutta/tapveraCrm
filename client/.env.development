# ========================================
# DEVELOPMENT ENVIRONMENT VARIABLES
# ========================================
# Automatically loaded by Vite in development mode

# Local Development Server
VITE_API_BASE=http://localhost:5000
VITE_WS_BASE=ws://localhost:5000

# NOTES:
# - Server should be running on port 5000
# - WebSocket connects at root (no /ws path needed)
# - Use http/ws for local development (no SSL required)
